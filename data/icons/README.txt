Replace/add these PNG files for the top tab icons:

crop-trim-rotate.png
adjust-image.png
adjust-audio.png
join-clips.png

Recommended size: 64x64 or 96x96 PNG with transparency.
The app displays them in a 64×64 slot above each tab label.
